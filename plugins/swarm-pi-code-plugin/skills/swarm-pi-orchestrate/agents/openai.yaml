interface:
  display_name: "Swarm Pi Orchestrate"
  short_description: "Compare bounded Pi perspectives on a decision"
  default_prompt: "Use $swarm-pi-orchestrate to compare bounded read-only perspectives on this decision."
