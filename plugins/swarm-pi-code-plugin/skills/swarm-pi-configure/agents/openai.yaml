interface:
  display_name: "Swarm Pi Configure"
  short_description: "Configure providers, models, and Pi project policy"
  default_prompt: "Use $swarm-pi-configure to configure Pi providers, models, and project policy."
