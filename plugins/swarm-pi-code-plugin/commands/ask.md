---
description: Delegate a focused repository question or read-only analysis to Pi
argument-hint: '<question>'
allowed-tools: Bash, Read, Write, AskUserQuestion
---

Use the bundled `swarm-pi-code-plugin-ask` skill with `$ARGUMENTS` as the complete question. Keep the result read-only and validate Pi's answer against repository evidence.
