---
description: Draft a repository-grounded implementation or architecture plan with Pi
argument-hint: '<planning request>'
allowed-tools: Bash, Read, Write, AskUserQuestion
---

Use the bundled `swarm-pi-code-plugin-plan` skill with `$ARGUMENTS` as the planning brief. Do not modify files or invoke implementation from this command.
