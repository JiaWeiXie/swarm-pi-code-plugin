---
description: Design and create a verified new project through isolated Pi staging
argument-hint: '<project request>'
allowed-tools: Bash, Read, Write, AskUserQuestion
---

Use the bundled `swarm-pi-code-plugin-scaffold` skill with `$ARGUMENTS` as the project request. Preserve the request while configuration is completed, obtain approval for the ScaffoldSpec, and materialize only a verified deliverable artifact.
