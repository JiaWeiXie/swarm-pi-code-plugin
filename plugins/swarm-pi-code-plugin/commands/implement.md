---
description: Apply an explicit scoped code or documentation change through Pi
argument-hint: '<approved change request>'
allowed-tools: Bash(node:*), Read, Write, AskUserQuestion, Skill
---

Use the bundled `swarm-pi-code-plugin-implement` skill with `$ARGUMENTS` as the approved scope. Never hide, stash, discard, commit, merge, push, or deploy user work.
