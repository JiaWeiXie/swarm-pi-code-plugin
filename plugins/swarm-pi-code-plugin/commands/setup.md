---
description: Configure project-local development tooling with the Pi environment engineer
argument-hint: '<setup request>'
allowed-tools: Bash, Read, Write, AskUserQuestion
---

Use the supervised `swarm-pi-code-plugin-setup` skill with `$ARGUMENTS` as the project-local tooling request. Never perform global provisioning, deployment, automatic cleanup, or Git delivery.
