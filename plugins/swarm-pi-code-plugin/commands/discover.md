---
description: Research unknown requirements through a gated, evidence-backed discovery workflow
argument-hint: '<discovery request>'
allowed-tools: Bash(node:*), Read, Write, AskUserQuestion, Skill
---

Use the bundled `swarm-pi-code-plugin-discover` skill with `$ARGUMENTS` as the discovery brief. Require reproducible, testable, evidence-backed experiments and do not materialize experiment artifacts.
